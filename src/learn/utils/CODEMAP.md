# CODEMAP.md — `src/learn/utils/`

Small generic helpers used by `src/learn/engine/`.

---

## Files

| File | Purpose |
| --- | --- |
| `shuffle.ts` | Exports `shuffle<T>(arr: T[]): T[]` — Fisher-Yates shuffle, returns a new array, does not mutate the input |
